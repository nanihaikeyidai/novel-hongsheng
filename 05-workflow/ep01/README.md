# 《红绳》第 1 集交付目录

本集按“片段交付、公共素材”两层归档，避免每个片段重复保存同一套人物、场景和音色文件，同时保证单个片段的提示词与专属素材可以就地查找。

- `红绳片段N/`：集中保存该段的剧本、成片、连续性台账、视频提示词、素材清单，以及该段独有的故事板、首帧、镜头参考或技能图。
- `归档素材/`：全集共用的人物人设、雨天教室、座位说明图、怪物与参考音色，每项仅保留一份。

使用视频生成平台时，仍以各段提示词和 `素材清单.md` 中的 `@图片N`、`@音频N` 槽位顺序上传；公共文件从 `归档素材/` 选择，片段专属文件直接从对应 `红绳片段N/` 选择。
